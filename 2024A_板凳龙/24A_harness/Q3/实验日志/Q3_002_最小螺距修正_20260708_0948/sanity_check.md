# Sanity Check：Q3 实验 002

| 项 | 判定 | 说明 |
|---|---|---|
| S1 量纲 | ✓ | 螺距 p、半径 R 均为 m；$\theta$ 弧度；$b=p/2\pi$ 量纲 m/rad 自洽 |
| S2 边界退化 | ✓ | $f(0.55)>0$（与 Q2 一致，碰撞在更内圈）；$p_{\min}=0.4503$ m < 0.55 m，符合预期 |
| S3 数量级 | ✓ | $p_{\min}\approx0.45$ m 介于板宽 0.30 m 与问1/2螺距 0.55 m 之间，量级合理；与该题公认答案（约 0.45 m）吻合 |
| S4 结构性 | ✓ | $m(p)$ 对 p 单调递增且近似线性（斜率≈1，见图3-2），根唯一；临界接触对(1,20)在龙头附近，与 Q2 一致的物理图像（龙头内侧最易碰） |
| S5 精度 | ✓ | `descent_min_clearance` 内置两级流程：粗网格（240点/3圈）定位最小值区间 + `minimize_scalar`(xatol=1e-10) 连续精化消除网格量化噪声，`m_of_pitch` 与外层 `brentq`(xtol=1e-9) 全程统一使用精化值，杜绝"网格版/精化版"数值不一致；复算 $m(p_{\min})=1.13\times10^{-14}\approx0$，验证根的精度 |
| S6 可视化自检 | ✓ | 临界构型图（图3-1）：龙头板凳与第19节龙身板凳恰好相切，边界圆 r=4.5m 内龙头前把手恰在边界附近 |

## 关键：建模判据的迭代（本问核心经验）

- **Q3_001（初版）判据不充分**：只要求"龙头到边界处 $G=0$"，得 $p=0.4202$ m。后验证发现间隙 $G$ 随盘入**振荡下降**，最紧接触在边界外侧（$r=4.58$m），即碰撞在龙头到达边界**之前**发生 → 初版低估螺距。
- **Q3_002（修正）判据**：要求"盘入到边界**全程** $\min G=0$"，得 $p_{\min}=0.4503$ m。此即物理正确的最小螺距。
- 教训对应假设 Q3-A04 标注的"单调性需后验证"——不默认 $G$ 单调是关键，避免了约 0.03 m 的系统性低估。

## 代码审查后修订记录（question-reviewer/code-reviewer loop 第1轮）

- **发现问题**：`code-reviewer` 复核时发现代码实际输出 $p_{\min}=0.450334$ m（纯网格版），与 README/params/总结/图注反复引用的 $0.450337$ m 不一致；文档提及的"连续精化"脚本在 `code/` 中缺失，构成可复现性缺口。
- **修复**：将 `minimize_scalar` 连续精化正式并入 `descent_min_clearance`（而非仅存在于文档描述），使 `m_of_pitch` 全程输出精化值。重新运行后代码稳定输出 $p_{\min}=0.450337$ m（与历史文档记录一致），$m(p_{\min})=1.13\times10^{-14}$，确认原文档数字是正确的、只是当时代码未落盘保存精化步骤。
- **验证**：重新生成 `outputs/q3_summary.csv`、`fig3-1`、`fig3-2`，与 README/params.md/总结文档中的数字逐一核对一致。

### 总判定：通过

修正判据物理正确，结果稳健（与公认答案一致），且经代码审查发现并修复了精化步骤的可复现性缺口，现已全流程自洽可复现。判碰函数已可跨螺距复用（问题3验证）。
