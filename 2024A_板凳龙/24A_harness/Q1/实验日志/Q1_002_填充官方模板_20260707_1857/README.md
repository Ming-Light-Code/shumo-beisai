# Q1_002 填充官方模板

- **目的**：用户放入官方模板后，将 Q1 结果直接填入 `题目/result1.xlsx` 官方模板，保证零改动合规。
- **与上次(Q1_001)差异**：仅改 `_write_excel` —— 由"新建工作簿手打标签"改为"load 官方模板、只写数据单元格"，模型/数值完全不变。
- **结果一句话**：与官方模板逐格比对，表头/第1列标签 100% 一致、数据无空缺、抽样数值与 Q1_001 相同；合规自检通过。

## 关键映射
- 位置 sheet：node i → 行 (2+2i)=x、(3+2i)=y；数据列 2..302 = 0..300 s。
- 速度 sheet：node i → 行 (2+i)；数据列 2..302。
- node 0=龙头前把手，node k=第k节龙身前把手(k=1..221)，node 222=龙尾(前)，node 223=龙尾(后)。

## 合规自检结果
- sheet 名一致（位置/速度）✓
- 表头(第1行)/标签(第1列)零改动 ✓（逐格 diff 无差异）
- 数据空单元格 = 0 ✓
- 抽样：龙头x@0s=8.8，龙尾(后)y@300s=9.301164，龙头v@0s=1，龙尾(后)v@300s=0.996478 ✓
