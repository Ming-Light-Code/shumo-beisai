import sys
sys.stdout.reconfigure(encoding='utf-8')

content = r"""# 解法二（DP/DFS）：问题2 在线天气决策模型 论证与说明

---

## 一、问题重述

任务2要求在任务1的网格地图与资源参数基础上引入**天气影响**。天气分为正常和雷暴两类，消耗率如下：

| 行为 | 正常天气 (O,H,F) | 雷暴天气 (O,H,F) |
|------|-----------------|-----------------|
| 移动一格 | (2,3,2) | (8,4,3) |
| 停泊（不作业） | (1,1,1) | (3,3,2) |
| 作业 | (5,4,3) | (8,6,6) |

**关键约束**：船舶在每日行动开始前**仅能观测当日天气**，无法预知未来天气。因此需要根据当前所在位置、剩余资源、资金余额、连续作业状态和当日天气，**实时制定当日行动决策**。

这是一个**在线决策问题**。题目特别要求：以**连续30天均为雷暴天气的极端天气下**，给出最优航行方案。优化目标同任务1：字典序 max Z，次 max M。

### 1.1 在线决策与离线规划的区别

在任务1（无天气）中，所有参数确定，最优方案可一次性离线求出。但在任务2中：

- **天气不可预知**：第 d 天行动前才知道 w_d 是正常还是雷暴，无法提前为未来天气定制路径
- **决策必须实时**：策略 pi(s_d, w_d) -> a_d 必须对任意可能的天气序列都有定义
- **全雷暴是特例**：当 w_d 恒为雷暴时，天气序列退化为确定型，在线策略的最优行为等同于该确定型问题的最优离线解

因此，任务2的正确解法框架是：**先建立通用的在线决策模型，再针对全雷暴确定型特例求解**。

### 1.2 雷暴消耗率对可行域的影响

雷暴消耗率远高于正常天气。以最短路径 B(1,5)->E(10,5)（9步）为例：
- 雷暴移动消耗：O=9x8=72, H=9x4=36, F=9x3=27
- 初始资源：O=35, H=45, F=30
- O 在第5天即耗尽

因此**直接航行不可能**，必须访问补给站。停泊消耗 (3,3,2) 约为正常停泊 (1,1,1) 的3倍，停泊-再作业策略的代价也大幅增加。

---

## 二、在线决策模型框架

### 2.1 状态空间

在第 d 天决策时，船舶掌握以下信息：

```
s_d = (x_d, y_d, O_d, H_d, F_d, M_d, Z_d, c_d, w_d)
```

其中 (x_d,y_d) 为当前位置，O_d/H_d/F_d 为自持资源存量，M_d 为资金，Z_d 为目标物资，c_d 为当前已连续作业天数，w_d 属于 {正常, 雷暴} 为当日观测到的天气。

### 2.2 动作空间

每日动作 a_d 取自：

```
A(s_d, w_d) = {
    移动 (上下左右 之一),         // 消耗 CM(w_d)，变位
    停泊 (原地不作业),            // 消耗 CI(w_d)，不变位，c 清零
    作业 (仅限位于 W_i 时),       // 消耗 CW(w_d)，收益 WY_i，c++
    补给 (仅限位于 S_j 时)        // 采购物资，不变位
}
```

其中作业受连续上限约束：若 c_d >= WM(i) 则当日不可选作业（除非前一日的 c 已通过停泊清零）。

### 2.3 在线决策策略

策略 pi 是一个函数：

```
pi: (s_d, w_d) -> a_d
```

给定策略 pi 和天气序列 W = (w_1, ..., w_30)，可向前模拟得到终止状态 (Z_final, M_final)。策略的优劣取决于其在**所有可能天气序列**下的表现。

### 2.4 全雷暴特例的退化

当 w_d 恒为雷暴时，天气序列确定。此时：

- 策略 pi 的模拟轨迹退化为一条**确定性路径**（路径 + 每日动作）
- 最优策略 pi* 等价于**该确定型场景下的离线最优解**
- 计算 pi* 等同于求解一个确定型资源约束路径规划问题

**结论**：全雷暴特例的求解，就是在雷暴消耗率下对路径骨架和作业天数做全局搜索，找最优确定型方案。这正是 DFS + 贪婪模拟所做的事。

---

## 三、全雷暴特例的求解方法

### 3.1 总体结构

```
输入: 全雷暴消耗率 (CM, CW, CI)
过程: DFS 枚举所有 (路径骨架, 工作天数) -> 贪婪模拟评估 -> 保留最优
输出: 最优确定型方案 (完整路径 + 每日动作 + 资源轨迹)
```

采用与问题1解法二一致的 **DFS + 上界剪枝 + 叶子节点贪婪前向模拟** 架构，对雷暴场景做消耗率参数化和停泊日支持两处关键适配。

### 3.2 DFS 路径枚举（含停泊日）

从 B(1) 出发，每步尝试去任一中转点（W1,W2,W3,S1,S2）或直接去 E。关键差异在于工作点的枚举：

对工作点 (类型 wh，连续上限 WM(wh))，工作天数 w 不再被 WM 硬限制。w 天作业需要 I(w) = max(0, ceil(w/WM)-1) 天停泊来打断连续作业链。因此总停留天数为 w + I(w)，枚举上限为：

```
W_max = max { w | w + I(w) <= 30 - day_used }
```

DFS 对每个 w 属于 [0, W_max] 递归，传入 day_used + w + I(w) 和 Z_so_far + w * WY(wh)。

**示例 (W3, WM=3, 到达后剩余10天)**：

| w | I(w) | w+I(w) | 可行 |
|---|------|--------|------|
| 6 | 1 | 7 | Y |
| 7 | 2 | 9 | Y |
| 8 | 2 | 10 | Y |
| 9 | 2 | 11 | N |

W_max = 8。与问题1（W_max 恒为 WM=3）形成鲜明对比：停泊-再作业策略使单次访问可完成远超 WM 的工作天数。

### 3.3 贪婪前向模拟 (greedy_sim_full)

在叶子节点（到达 E），对完整路径和作业天数运行逐日模拟：

1. **日程构建**：按路径顺序填充移动日 (CM)、作业日 (CW)、停泊日 (CI)，同时标记补给日 (isSup)
2. **逐日执行**：每天先扣消耗，补给日再采购
3. **采购策略**：buy = max(0, need - current)，其中 need = 到下一补给日前一天的总消耗。此策略在确定型日程下已被证明为最优（见问题1论证）

总天数 T 的计算计入停泊日：

```
T = total_travel + Sum_j [ w_j + max(0, ceil(w_j / WM_j) - 1) ]
```

---

## 四、正确性论证

### 4.1 路径空间完备性

**命题**：DFS 探索了所有满足 day <= 30 的点序列（允许重复访问，无位掩码限制）。

**论证**：DFS 从 B 出发，每步尝试所有 5 个中间点和终点 E。day <= 30 的边界检查在分支前执行。无访问次数限制。（证毕）

### 4.2 工作天数完备性（含停泊日）

**命题**：对路径中每个工作点，所有满足总天数约束的工作天数组合均被 DFS 枚举。

**论证**：DFS 计算 W_max = max { w | w + I(w) <= D_remain }，然后枚举 w 属于 [0, W_max]。任意可行工作天数 w 必满足 w + I(w) <= D_remain，从而 w <= W_max。（证毕）

### 4.3 停泊日数量的最优性

**命题**：对于总工作天数 w，I(w) = max(0, ceil(w/WM)-1) 是最小必要停泊日数。

**论证**：完成 w 天作业需至少 ceil(w/WM) 个连续作业段。段间至少需 1 天非作业日。最少停泊日数为 ceil(w/WM)-1。（证毕）

### 4.4 贪婪采购最优性（雷暴不变性）

**命题**：在确定型日程下，buy_i = max(0, need_i - current_i) 仍为最优采购策略。

**论证**：未来消耗 need 确定，每种资源的 deficit 独立。buy 取各资源硬下界的并集即为最优。雷暴天气仅改变数值，未改变问题的结构（无跨资源替代，无隐藏决策自由度）。（证毕）

### 4.5 在线策略与全雷暴最优解的一致性

**命题**：DFS 求得的确定型最优方案，就是在线策略 pi* 在全雷暴天气序列下的执行结果。

**论证**：当 w_d 恒为雷暴时，pi 的模拟轨迹退化为一条确定型路径。若存在另一策略 pi' 产生更好的 (Z,M)，则其模拟轨迹也是一条确定型路径，且在 DFS 搜索空间内会被探索并评估。DFS 保留全局最优，pi* 的结果不会被超越。（证毕）

### 4.6 剩余风险

1. **<= 剪枝的字典序边界**：当 Z_so_far + remain * 28 == best_Z 时剪枝，可能丢失 Z 相同但 M 更高的解。
2. **上界高估**：剪枝上界未扣除停泊日开销和资源约束，是乐观上界（安全剪枝），但效率有所下降。
3. **资源对剪枝的不可见性**：DFS 不跟踪资源状态，资源不可行路径需到叶子节点才被否决。

---

## 五、复杂度分析

### 5.1 搜索空间对比

| 维度 | 问题1（正常天气） | 问题2（雷暴天气） |
|------|------------------|-------------------|
| 分支因子 | 约8 | 约10（W枚举范围因停泊日扩大） |
| 有效深度 | 偏长（移动成本低） | 偏短（移动成本高，资源压力大） |
| 预计节点数 | 10^5-10^7 | 10^5-10^7 |

移动成本增大约4倍使可行路径长度缩短，部分抵消 W 枚举范围扩大的增长。

### 5.2 空间复杂度

DFS 递归深度 <= 路径长度 <= 15。空间 O(深度) 约等于 O(30)，内存友好。

---

## 六、与解法一的关系

两种方法在核心思想上一致（枚举路径 + 枚举工作天数 + 贪婪模拟评估），区别在于实现形式：

| 方面 | 解法一（枚举+贪婪） | 解法二（DP/DFS） |
|------|-------------------|-----------------|
| 路径生成 | 三层嵌套循环，5^k 进制展开 | DFS 递归 |
| 工作天数 | 乘积空间枚举 | DFS 内嵌 for 循环 |
| 剪枝 | 仅天数上限 | 天数上限 + Z 上界 |
| 搜索完备性 | seq_len <= K_max 内保证 | 全空间保证 |

两者在全雷暴特例上应输出相同的最优解。解法二的上界剪枝可额外减少探索量。

---

## 七、使用说明

### 运行方式

```matlab
cd('C:\\Users\\ming\\Desktop\\数模备赛\\解法二_DP完善');
[Z, M, path, wdays] = method_dp_complete_task2();
```

### 输出示例

```
探索节点数: XXXXX
=== 解法二 (DP/DFS) 问题2 最优结果 ===
Z = XXX, M = XXX
路径: B -> S1 -> W3 -> S2 -> W3 -> E
工作天数: 0 3 0 2
```

- wdays 仅列出工作点作业天数（不含停泊日），按路径中工作点出现顺序排列
- 停泊日由 greedy_sim_full 根据作业天数和 WM 自动计算插入
- 可与 task2_solution.m 的枚举+贪婪方法输出对照验证

---

*文档生成日期：2026-07-19*
*对应代码文件：method_dp_complete_task2.m*
"""

with open(r'C:\Users\ming\Desktop\数模备赛\解法二_DP完善\解法二_问题2_论证与说明.md', 'w', encoding='utf-8') as f:
    f.write(content)
print('Markdown file written successfully.')
