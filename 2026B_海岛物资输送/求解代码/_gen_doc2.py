import sys
sys.stdout.reconfigure(encoding="utf-8")

doc = """# 问题一 解法说明：枚举 + 贪婪前向模拟（含停泊-再作业策略）

---

## 一、问题重述与抽象

在 10x10 的网格海域中，船舶从起始岛 B(1,5) 出发，须在 30 天内抵达终点交付岛 E(10,5)。海域中有 5 个特殊功能点：

| 编号 | 名称 | 坐标 | 功能 |
|------|------|------|------|
| 3 | W1 | (2,7) | 作业点，收益 20/天，最多连续 4 天 |
| 4 | W2 | (5,3) | 作业点，收益 15/天，最多连续 5 天 |
| 5 | W3 | (8,8) | 作业点，收益 28/天，最多连续 3 天 |
| 6 | S1 | (3,4) | 补给平台，可采购资源 |
| 7 | S2 | (7,6) | 补给平台，可采购资源 |

初始资源：燃油 O=35，淡水 H=45，食物 F=30，资金 M=240，目标物资 Z=100。
载重上限 L=120 单位。采购价格：p_O=2, p_H=1, p_F=2（资金/单位）。

消耗规则：

| 行为 | O | H | F | 说明 |
|---|---|---|---|---|
| 移动一格 | 2 | 3 | 2 | 曼哈顿移动 |
| 原地停泊 | 1 | 1 | 1 | 用于打断连续作业链，突破单次最多连续作业限制 |
| 作业一天 | 5 | 4 | 3 | 获得该点收益 |

优化目标（字典序）：max Z，次 max M。

---

## 二、数学模型

### 2.1 三层嵌套优化框架

将问题分解为三个嵌套决策层：

**外层 —— 路径骨架：** 设中间点集合 P = {W1, W2, W3, S1, S2}，路径骨架为序列

```
P = [B, p_1, p_2, ..., p_k, E]
```

其中 p_i ∈ P 且 p_i ≠ p_{i+1}（相邻重复点需过滤），k ∈ [0, K_max]。

相邻重复点过滤的必要性：若允许 W3→W3，距离为 0，将绕过"单次最多连续作业 3 天"的约束（相当于连续作业 6 天而只计入两次 3 天），因此必须过滤。

K_max 的理论上界由总天数约束导出。记最小段距离：
- min d(B, P) = 3（B→S1 或 B→W1）
- min d(P, P) = 3（S1↔W2 或 W3↔S2）
- min d(P, E) = 4（S2→E）

则 k 个中间点的最小总旅行天数 T_travel_min(k) = 3 + 3(k-1) + 4 = 3k + 4。由 T_travel_min(k) ≤ 30 解得 k ≤ 8.67，故 K_max = 8。

**中层 —— 工作天数与停泊策略：** 对路径中第 j 个作业点（按出现顺序），决策在该点的总工作天数 W_j。关键在于，题目规定"单次最多连续作业 D_j_max 天"——这意味着连续作业 D_j_max 天后，可以通过插入 1 天停泊（原地不作业，消耗 CI=(1,1,1)）打断连续链，然后继续作业。

停泊日消耗 (1,1,1) 远低于作业消耗 (5,4,3)，虽不产生 Z 收益，但换来了在同一访问点继续作业的资格。这比"离开作业点→去补给站→返回作业点"的往返策略更高效：
- 往返 W3→S2→W3 需 6 天移动（消耗 6×CM=(12,18,12)），加上 6 天作业（总计 12 天，得 6×28=168 Z）
- 停泊打断：3 天作业→1 天停泊→3 天作业（总计 7 天，消耗 6×CW+(1,1,1)=(31,25,19)，得 168 Z）
- 停泊策略少用 5 天，少消耗 (12+31, 18+25, 12+19) vs (42,42,30) 各维度均更优

对于给定的总工作天数 W_j，最优分段策略是：
- 用最少的停泊日打断连续作业链
- 每段作业天数尽可能利用 D_j_max 上限
- 停泊日数 I_j = max(0, ceil(W_j / D_j_max) - 1)
- 总停留天数 = W_j + I_j

**内层 —— 采购决策：** 给定骨架 P 和工作天数向量 W（以及由此确定的停泊日），逐日日程完全确定。在补给日 t，决策采购量。

### 2.2 停泊-再作业策略的最优性

**定理：** 在确定性日程下，对于给定的总工作天数 W_j，使停泊日数 I_j = max(0, ceil(W_j/D_j_max)-1) 的分段策略是最优的（在停留天数最少的意义下）。

**证明：** 要完成 W_j 天作业，至少需要 ceil(W_j/D_j_max) 个连续作业段。连续段之间至少需要 1 天非作业日来打断连续链（题目规定"单次最多连续作业"的约束对象是连续作业日，停泊日不计入连续作业）。因此所需停泊日最少为 ceil(W_j/D_j_max)-1，即 I_j。每段作业天数取 D_j_max 填满前几段即可实现此下界。证毕。

**停泊日为何只取 1 天而非更多：** 停泊日消耗资源而不产生 Z，多用停泊日只会浪费天数且多耗资源。1 天是打断连续链的最小必要天数。因此每个连续段间的停泊日数恒为 1。

### 2.3 采购子问题的形式化

设在补给日 t，当前资源存量为 s = (O_t, H_t, F_t)，资金为 M_t，下一个补给日为 t_next（若为最后一个补给站，则 t_next = T+1）。从 t+1 到 t_next 期间（含 t_next 当日）的累计消耗向量（含移动、作业、停泊）记为 need。

采购问题为：

```
min  cost = 2*buy_O + 1*buy_H + 2*buy_F
s.t. s + buy >= need                （存活约束）
     sum(buy) <= 120 - sum(s)       （载重约束）
     cost <= M_t                     （资金约束）
     buy >= 0
```

### 2.4 采购贪婪策略的最优性证明

**定理：** 在确定性的日程下，buy_i = max(0, need_i - s_i) 是上述采购问题的最优解（若可行），且任一资源不存在其他可行解能减少采购量。

**证明：** 由于消耗确定且不可规避，每个资源 i 在到达下一补给站之前需满足 s_i + buy_i >= need_i，即 buy_i >= need_i - s_i。结合非负约束得 buy_i >= max(0, need_i - s_i) =: buy_i_min。

总采购量下界为 sum(buy_i_min)，总成本下界为 sum(p_i * buy_i_min)。解 buy_min 同时使总采购量和总成本达到各自的下界，且在每个分量上均不可能进一步减小——任何分量减小都将导致该资源在途中耗尽。

若 sum(buy_i_min) > 120 - sum(s_i)（载重不足），则所有可行解的总采购量均大于等于此下界，从而不存在可行采购方案，日程不可行。证毕。

**重要澄清 —— 三种资源共享载重约束：** 任一资源的购买量必须 >= 其硬下界，而总下界 sum(buy_i_min) 是总采购量的最小值。若该最小值已超出可用载重，不存在配比调整能使总采购量降到载重内。不存在"提前在上一补给站多买"的策略空间，因为提前多买同样占用载重，只是将载重压力平移。

---

## 三、算法设计

### 3.1 总算法流程

```
best_Z = -inf, best_M = -inf

for seq_len = 0 to K_max:                     // 外层：枚举路径骨架
    for each 序列 seq in {1..5}^seq_len:
        P = [B, decode(seq), E]
        if P has adjacent duplicate: continue
        total_travel = sum dist(P[i], P[i+1])
        if total_travel > 30: continue
        
        identify work point sequence in P
        D_avail = 30 - total_travel            // 可用于作业+停泊的天数
        
        for each work point visit j:            // 计算枚举上限
            W_max_j = max w s.t. w + max(0, ceil(w/D_j_max)-1) <= D_avail
        
        for each work-day combo W[1..n_w]:      // 中层：枚举总工作天数
            total_days = total_travel + sum(W + idle)
            if total_days > 30: continue        // 含停泊日的总天数剪枝
            
            (feas, Z, M) = GreedySim(P, W)      // 内层：贪婪模拟（含停泊日）
            if feas and (Z,M) better than best: update best
```

### 3.2 贪婪前向模拟 (GreedySim)

日程构建时，工作天数 W_j 被分解为多段：每段 min(剩余, D_j_max) 天作业，段间插入 1 天停泊。总天数 T 已包含停泊日。

```
// === 日程构建（含停泊日） ===
T = total_travel
for each work visit j:
    T = T + W_j + max(0, ceil(W_j/D_j_max) - 1)   // 作业日 + 停泊日

// 逐日推进
O = 35, H = 45, F = 30, M = 240
for t = 1 to T:
    // Step 1: 无条件扣除当日消耗
    O = O - c_O(t);  H = H - c_H(t);  F = F - c_F(t)
    if O<0 or H<0 or F<0: return infeasible
    
    if isSup[t]:                                  // 补给日
        find next supply day nextSup
        need = sum_{tau=t+1}^{nextSup} c(tau)     // 含移动/作业/停泊消耗
        buy = max(0, need - (O,H,F))
        if sum(buy) > 120 - (O+H+F): return infeasible
        if cost(buy) > M: return infeasible
        O += buy_O; H += buy_H; F += buy_F; M -= cost(buy)
    
    if M<0 or O+H+F>120: return infeasible

Z = 100 + sum(zGain); M_final = M; return feasible
```

### 3.3 关键设计决策

**停泊-再作业策略的引入。** 题目"单次最多连续作业"的约束对象是"连续"——作业日与非作业日（停泊日）交替即打断连续链。插入 1 天停泊（消耗 CI=(1,1,1)，比作业消耗 CW=(5,4,3) 低得多）即可在同一访问点继续作业。枚举中工作天数 W_j 不再受 D_j_max 上界约束，而是受限于可用总天数（含停泊日）。

**先消耗、再采购的时间顺序（Bug 修复）。** 旧版在补给日跳过消耗扣除，导致首个补给站到达日移动消耗遗漏。修复后每天先无条件扣消耗，补给日再采购。整条资源链闭合无缺口。

**工作天数枚举上界的动态计算。** 对每个作业点访问，W_max 由"W + max(0, ceil(W/D_max)-1) ≤ 可用天数"的动态计算确定。例如可用 15 天、W3 (D_max=3)：W_max = 12（12 作业 + 3 停泊 = 15 天）。

**路径骨架的进制展开枚举。** 用 5 进制展开生成所有中间点序列，通过相邻重复过滤和总旅行天数过滤裁剪。该方法的完备性不依赖于启发式或剪枝。

---

## 四、复杂度分析

### 4.1 搜索空间规模

| 层级 | 原方案（无停泊） | 本方案（含停泊） |
|---|---|---|
| 路径骨架（过滤后） | ~5e4 | ~5e4 |
| 每作业点 W 枚举范围 | [0, D_max] = 4~6 值 | [0, W_max] = 13~16 值 |
| 每骨架组合数（2 次访问） | ~20 | ~200 |
| 贪婪模拟调用总次数 | ~1e6 | ~1e7 |
| MATLAB 预估总耗时 | 30-120 秒 | 3-10 分钟 |

### 4.2 完备性论证

本方案在三个维度均为完备穷举：
- **路径骨架**：5^k 全排列覆盖所有特殊点序列，K_max=8 覆盖理论最大访问次数
- **总作业天数**：W_j 从 0 到 W_max_j 全覆盖，W_max_j 由可用天数精确约束
- **停泊日数**：由 W_j 和 D_j_max 经闭式公式唯一确定，无额外枚举维度

非特殊网格点无功能，曼哈顿最短路径抽象不遗漏更优解。综合以上，**本算法在枚举空间内保证找到全局最优解**。

### 4.3 与无停泊方案的理论增益

以 W3 (收益 28/天, 限连续 3 天) 为例：
- 无停泊：每次访问最多 3 天作业 = 84 Z
- 含停泊（可用 12 天）：可作业 9 天（含 3 天停泊）= 252 Z，增益 200%

同理 W1 (收益 20/天, 限连续 4 天)：每次访问最多 12 天作业（含 2 天停泊）= 240 Z vs 无停泊 80 Z。

---

## 五、输出说明

运行 task1_solution 后将输出三组结果，方案一为含停泊策略的精确全局最优。输出包括：

- 最优 Z 和 M 值
- 路径骨架（特殊功能点序列）
- 总旅行天数、总作业天数
- 逐日详细日程表，包含：
  - 每日结束时的网格坐标
  - 每日决策行为（Move / Move->name / Supply / Work@name / Idle）
  - 每日结束时的资源存量（O, H, F, M, Z）
  - 补给日的采购量

**工具包依赖：** 仅需 MATLAB 基础环境，无需 Optimization Toolbox。

---

## 六、方法对比简述

| 方法 | 正确性 | 完备性 | 说明 |
|---|---|---|---|
| 方案一（枚举+贪婪+停泊） | 正确 | 完备（全局最优） | 本方案 |
| 方案二（DP） | 无法求解 | 不完备 | 单步转移，不累积路径，输出 Z=100 |
| 方案三（DE） | 启发式 | 无保证 | 差分进化，编码限制 6 个中间点 |

方案二的最优子结构在本问题中不成立——到达同一状态的不同路径有不同资源向量。
方案三仅作启发式参考，不保证最优解。

**方案一是三者中唯一能给出精确全局最优解的方法。**
"""

with open(r"C:\Users\ming\Desktop\数模代码\解法说明.md", "w", encoding="utf-8") as f:
    f.write(doc)
print(f"Document written: {len(doc)} chars")
