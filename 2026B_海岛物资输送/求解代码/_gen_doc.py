import sys
sys.stdout.reconfigure(encoding="utf-8")

doc = """# 问题一 解法说明：枚举 + 贪婪前向模拟

---

## 一、问题重述与抽象

在 10x10 的网格海域中，船舶从起始岛 B(1,5) 出发，须在 30 天内抵达终点交付岛 E(10,5)。海域中有 5 个特殊功能点：

| 编号 | 名称 | 坐标 | 功能 |
|------|------|------|------|
| 3 | W1 | (2,7) | 作业点，收益 20/天，最多连续 4 天 |
| 4 | W2 | (5,3) | 作业点，收益 15/天，最多连续 5 天 |
| 5 | W3 | (8,8) | 作业点，收益 28/天，最多连续 3 天 |
| 6 | S1 | (3,4) | 补给平台，可采购资源 |
| 7 | S2 | (7,6) | 补给平台，可采购资源 |

初始资源：燃油 O=35，淡水 H=45，食物 F=30，资金 M=240，目标物资 Z=100。
载重上限 L=120 单位。采购价格：p_O=2, p_H=1, p_F=2（资金/单位）。

消耗规则：

| 行为 | O | H | F | 说明 |
|---|---|---|---|---|
| 移动一格 | 2 | 3 | 2 | 曼哈顿移动 |
| 原地停泊 | 1 | 1 | 1 | 本方案不使用（无天气时无益） |
| 作业一天 | 5 | 4 | 3 | 获得该点收益 |

优化目标（字典序）：max Z，次 max M。

---

## 二、数学模型

### 2.1 三层嵌套优化框架

将问题分解为三个嵌套决策层：

**外层 —— 路径骨架：** 设中间点集合 P = {W1, W2, W3, S1, S2}，路径骨架为序列

```
P = [B, p_1, p_2, ..., p_k, E]
```

其中 p_i ∈ P 且 p_i ≠ p_{i+1}（相邻重复点需过滤），k ∈ [0, K_max]。

相邻重复点过滤的必要性：若允许 W3→W3，距离为 0，将绕过"单次最多连续作业 3 天"的约束（相当于连续作业 6 天而只计入两次 3 天），因此必须过滤。

K_max 的理论上界由总天数约束导出。记最小段距离：
- min d(B, P) = 3（B→S1 或 B→W1）
- min d(P, P) = 3（S1↔W2 或 W3↔S2）
- min d(P, E) = 4（S2→E）

则 k 个中间点的最小总旅行天数 T_travel_min(k) = 3 + 3(k-1) + 4 = 3k + 4。由 T_travel_min(k) ≤ 30 解得 k ≤ 8.67，故 K_max = 8。

**中层 —— 工作天数：** 对路径中第 j 个作业点（按出现顺序），决策在该点的工作天数 d_j：

```
d_j ∈ [0, D_j_max],  j = 1, ..., n_w
```

其中 D_j_max 为该作业点的最大连续作业上限（W1:4, W2:5, W3:3）。若路径中同一作业点出现多次，每次视为独立的 d_j 变量，天然满足"单次最多连续"约束。

**内层 —— 采购决策：** 给定骨架 P 和工作天数向量 d，逐日日程完全确定。在补给日 t，决策采购量 buy = (buy_O, buy_H, buy_F)。

### 2.2 采购子问题的形式化

设在补给日 t，当前资源存量为 s = (O_t, H_t, F_t)，资金为 M_t，下一个补给日为 t_next（若为最后一个补给站，则 t_next = T+1）。从 t+1 到 t_next 期间（含 t_next 当日）的累计消耗向量记为 need：

```
need = sum_{tau=t+1}^{t_next} c(tau)
```

其中 c(tau) ∈ {(2,3,2), (5,4,3)} 为第 tau 天的消耗向量。

采购问题为：

```
min  cost = 2*buy_O + 1*buy_H + 2*buy_F
s.t. s + buy >= need                （存活约束）
     sum(buy) <= 120 - sum(s)       （载重约束）
     cost <= M_t                     （资金约束）
     buy >= 0
```

### 2.3 采购贪婪策略的最优性证明

**定理：** 在确定性的日程下，buy_i = max(0, need_i - s_i) 是上述采购问题的最优解（若可行），且任一资源不存在其他可行解能减少采购量。

**证明：** 由于消耗确定且不可规避，每个资源 i 在到达下一补给站之前需满足 s_i + buy_i >= need_i，即 buy_i >= need_i - s_i。结合非负约束得 buy_i >= max(0, need_i - s_i)。记此下界为 buy_i_min。

总采购量下界为 sum(buy_i_min)，总成本下界为 sum(p_i * buy_i_min)。解 buy_min 同时使总采购量和总成本达到各自的下界，且在每个分量上均不可能进一步减小——任何分量减小都将导致该资源在途中耗尽，使日程不可行。

若 sum(buy_i_min) > 120 - sum(s_i)（载重不足），则所有可行解的总采购量均大于等于此下界，从而不存在可行采购方案，日程不可行。证毕。

**推论：** 贪婪策略对采购子问题是精确最优的，采购量由闭式公式 buy_min = max(0, need - s) 直接给出，无需数值求解线性规划。

**重要澄清 —— 三种资源共享载重约束：** 是否存在"调整各资源采购配比"来拯救不可行日程的可能？答案是否定的。任一资源的购买量必须 >= 其硬下界，而总下界 sum(buy_i_min) 是总采购量的最小值。若该最小值已超出可用载重，不存在配比调整能使总采购量降到载重内——因为每种资源都不能少买。不存在"提前在上一补给站多买"的策略空间，因为提前多买同样占用载重，只是将载重压力从后续补给站平移过来，不改变可行性。

---

## 三、算法设计

### 3.1 总算法流程

```
best_Z = -inf, best_M = -inf

for seq_len = 0 to K_max:                     // 外层：枚举路径骨架
    for each 序列 seq in {1..5}^seq_len:
        P = [B, decode(seq), E]
        if P has adjacent duplicate: continue
        total_travel = sum dist(P[i], P[i+1])
        if total_travel > 30: continue
        
        identify work point sequence in P
        
        for each work-day combo d[1..n_w]:     // 中层：枚举工作天数
            if total_travel + sum(d) > 30: continue
            
            (feas, Z, M) = GreedySim(P, d)    // 内层：贪婪模拟
            if feas and (Z,M) better than best:
                update best
```

### 3.2 贪婪前向模拟 (GreedySim)

对给定骨架 P 和工作天数 d，构建逐日日程表（每格记录消耗向量、是否补给日、作业收益），然后逐日推进：

```
O = 35, H = 45, F = 30, M = 240

for t = 1 to T:
    // Step 1: 无条件扣除当日消耗（修复后的逻辑）
    O = O - c_O(t);  H = H - c_H(t);  F = F - c_F(t)
    if O<0 or H<0 or F<0: return infeasible
    
    if isSup[t]:                                  // 补给日
        find next supply day nextSup
        need = sum_{tau=t+1}^{nextSup} c(tau)
        buy = max(0, need - (O,H,F))
        if sum(buy) > 120 - (O+H+F): return infeasible   // 载重不足
        if cost(buy) > M: return infeasible                // 资金不足
        O = O + buy_O; H = H + buy_H; F = F + buy_F
        M = M - cost(buy)
    
    if M<0 or O+H+F>120: return infeasible

Z = 100 + sum(zGain);  M_final = M
return feasible
```

### 3.3 关键设计决策

**先消耗、再采购的时间顺序（Bug 修复）。** 旧版代码在补给日跳过消耗扣除（else 分支），导致到达首个补给站的当日移动消耗 CM=(2,3,2) 从未被计入——这是一个边界 bug。修复后逻辑为：每天先无条件扣除当日消耗，再在补给日执行采购。到达首个补给站的消耗由初始资源正常覆盖；后续补给站的到达消耗由前一补给站的采购覆盖；最后补给站到终点的消耗由最后采购覆盖。整条资源链闭合无缺口。

**工作天数的枚举空间。** 三个作业点的工作天数上限分别为 [4,5,3]，乘积空间 5x6x4 = 120 种组合。在路径中同一作业点可能多次出现，每次获得独立的枚举变量，但总枚举量最大仍为 120 的倍数，完全在可控范围内。

**路径骨架的进制展开枚举。** 用 5 进制展开生成所有长度为 seq_len 的中间点序列，再通过相邻重复过滤和总旅行天数过滤裁剪不可行路径。该方法保证了搜索空间的完备覆盖，不依赖启发式或剪枝。

---

## 四、复杂度分析

### 4.1 搜索空间规模

| 层级 | 规模 |
|---|---|
| 路径骨架（seq_len 0~8，过滤相邻重复后） | ~1.1e5 |
| total_travel > 30 过滤后 | ~5e4 |
| 作业天数组合（每骨架，平均值） | ~20-30 |
| 贪婪模拟调用总次数 | ~1e6 - 1.5e6 |
| 单次贪婪模拟迭代量（T <= 30） | ~90 次基础操作 |
| MATLAB 预估总耗时 | 30-120 秒 |

### 4.2 完备性论证

对所有特殊功能点的所有可能访问序列进行了穷举（5^k 全排列 + 相邻重复过滤 + 总旅行天数过滤）。K_max=8 覆盖了 30 天约束下的理论最大访问次数——9 个中间点的最小总旅行天数为 31，已超出约束。

非特殊网格点不能补给也不能作业，绕路只会增加消耗——因此曼哈顿最短路径抽象不会遗漏更优解。无天气条件下原地停泊只会消耗资源而无收益，不可能出现在最优解中。

三层枚举在中层和内层均为有限空间的完备穷举。综合以上，**本算法在枚举空间内保证找到全局最优解**。

---

## 五、输出说明

运行 task1_solution 后将输出三组结果（枚举+贪婪、DP、DE），其中方案一（Method 1: Enumeration + Greedy）为精确全局最优。输出包括：

- 最优 Z 和 M 值
- 路径骨架（特殊功能点序列，如 B->S1->W2->W3->S2->E）
- 总旅行天数、总作业天数
- 逐日详细日程表，包含：
  - 每日结束时的网格坐标
  - 每日决策行为（Move / Move->name / Supply / Work@name）
  - 每日结束时的资源存量（O, H, F, M, Z）
  - 补给日的采购量（格式：(O, H, F)）

**工具包依赖：** 仅需 MATLAB 基础环境，无需 Optimization Toolbox 或其他附加工具箱。

---

## 六、方法对比简述

本文件中的方案二（DP）和方案三（DE）供参考对比：

| 方法 | 正确性 | 完备性 | 说明 |
|---|---|---|---|
| 方案一（枚举+贪婪） | 正确（已修复） | 完备（全局最优） | 本方案 |
| 方案二（DP） | 无法求解 | 不完备 | 单步转移 DP，不累积多段路径，仅输出 Z=100 |
| 方案三（DE） | 启发式 | 无保证 | 差分进化，染色体编码限制为最多 6 个中间点 |

方案二仅做 last->next 单段转移，调用 GreedySim 时总是从初始资源出发，不继承父状态资源。掩码 DP 的最优子结构在本问题中不成立——到达同一 (mask, last) 状态的不同路径可能具有完全不同的资源向量 (O,H,F,M)，仅比较 (Z,M) 不足以判断优劣。

方案三用差分进化（种群 300，150 代）在 10 维空间搜索。染色体中工作天数编码为每作业点一个值（而非按出现顺序），无法区分同一作业点的多次访问。搜索效果依赖随机性和参数调优，不保证找到最优解。

**方案一是三者中唯一能给出精确全局最优解的方法。**
"""

with open(r"C:\Users\ming\Desktop\数模代码\解法说明.md", "w", encoding="utf-8") as f:
    f.write(doc)
print("Document written: " + str(len(doc)) + " chars")
