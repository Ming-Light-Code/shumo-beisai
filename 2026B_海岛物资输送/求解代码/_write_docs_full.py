import os
desktop = os.path.join(os.environ['USERPROFILE'], 'Desktop')

cg_doc = """# 列生成方法求解海岛物资输送问题

## 一、算法概述

列生成（Column Generation）是一种用于求解大规模线性/整数规划的分解算法。其核心思想是：当问题的决策变量（"列"）数量极其庞大时，不一次性生成所有列，而是在求解过程中根据需要动态生成能改进目标函数的列。

对于本题，每一"列"代表一条**完整的航行方案**——包含路径序列、每日工作天数、各补给站的采购量。所有可行方案构成集合 R。主问题从 R 中恰好选择一条路径。

## 二、数学建模

### 2.1 主问题（Master Problem）

设 R 为所有可行路径的集合。对每条路径 r in R，定义 Z(r) 为抵达终点时携带的目标物资，M(r) 为剩余资金，a_r in {0,1} 为是否选择路径 r。

主问题为：max sum_{r in R} Z(r) * a_r，约束：sum_{r in R} a_r = 1, a_r in {0,1}。

这只有一条凸性约束（恰好选一条路径），其 LP 松弛的最优解平凡地等于 max_{r in R} Z(r)。LP 的最优对偶变量 pi = Z_max。

### 2.2 定价子问题（Pricing Subproblem）

对最大化问题，列 r 的检验数（reduced cost）为：bar{c}_r = Z(r) - pi。若存在路径 r 使得 Z(r) > Z_current，则加入该列可改进主问题目标。定价子问题即为：找到一条可行路径 r，使得 Z(r) > Z_current，或证明这样的路径不存在。

### 2.3 定价子问题的求解

定价子问题通过**骨架枚举 + 贪婪前向模拟**求解：

- **外层**：枚举路径骨架 B->{中间点序列}->E，过滤总旅行距离超过 30 天的骨架
- **中层**：枚举各作业点工作天数组合（0 到 max_consec），过滤总天数超过 30 的组合
- **内层**：贪婪前向模拟——逐日推进追踪资源，在补给日按"刚好够到下一个补给点"的策略采购，检查载重和非负约束。若资源可行，计算 Z(r)

若 Z(r) > Z_current，则找到改进列，加入主问题路径池。

## 三、算法流程

1. 初始化路径池 Pool = {B->E 直接路径}
2. Z* = max_{r in Pool} Z(r)
3. 循环：pi = Z*，搜索骨架空间找 Z(r) > pi 的新列。若找到则加入 Pool 并更新 Z*，否则终止。
4. 返回 Pool 中 Z 最大的路径。

## 四、收敛性分析

主问题只有一条约束，其 LP 松弛的极点解是单位向量。每次定价迭代找到的改进列严格提升目标值 Z*（因为检验数 > 0）。由于骨架空间有限（约 542 种有效序列），工作天数组合也有限，定价子问题在有限次迭代后必然收敛。

## 五、实验结果

| 指标 | 值 |
|------|:--:|
| 最优 Z | 288 |
| 最优 M | 44 |
| 路径 | B -> W1 -> S2 -> W3 -> S2 -> W3 -> E |
| 生成列数 | 15 |
| 定价迭代 | 16 |
| 耗时 | 0.17 秒 |

迭代过程显示 pi 从 100 单调递增到 268，再到 288，体现了路径池质量的逐步提升。

## 六、对偶变量的经济学解释

对偶变量 pi 代表"影子价格"——在当前路径池中，多一条可用路径最多能增加多少 Z。定价子问题检验数 Z(r) - pi > 0 的含义是：新路径 r 的 Z 值超过了当前池的"市场价"，值得加入。最终 pi 稳定在 268，再无新路径能超越（Z=288 的路径是最后加入的改进列）。

## 七、方法评价

**优点**：对偶理论保证全局最优；天然支持并行化（定价可并行搜索）；可扩展至更大规模（30x30 网格）；迭代过程直观展示解的逐步改进。

**局限**：本题枚举空间较小，列生成 overhead 大于纯枚举；定价子问题本身是 NP-hard 的 RCSP；主问题仅一条约束，退化严重。

## 八、与骨架枚举的对比

纯骨架枚举一次性评估所有 542 个骨架的所有工作天数组合（约 65,000 个），然后选最优。列生成通过"逐步改进"达到同样结果：第 15 次迭代找到最优列 Z=288，继续定价验证没有更好的列。在本题中两者时间几乎相同（0.17s vs 0.18s），但在 30x30 网格的任务 3 中，列生成的优势将显著体现。
"""

cp_doc = """# 约束规划方法求解海岛物资输送问题

## 一、算法概述

约束规划（Constraint Programming, CP）是一种用于求解组合优化问题的编程范式。与数学规划（LP/MILP）依赖线性松弛和目标函数梯度不同，CP 通过**约束传播 + 系统搜索**在离散决策变量的有限值域上寻找可行解并优化目标。

对于本题，CP 将航行方案建模为一系列离散决策——每天的位置、动作、采购量——受资源约束、载重约束、时间约束和连续作业上限约束的共同限制。CP 求解器通过递归回溯搜索所有可能的决策组合，同时利用约束传播和前向检查提前剪枝。

## 二、CP 的三个核心机制

### 2.1 约束传播（Constraint Propagation）

约束传播在搜索的每个节点自动删除变量值域中不可能满足约束的值。例如在本题中：若 O_t < 2，则 day t+1 不能移动（燃油不够）；若当前位置不在作业点，则 act_t 的值域删除 {W}；若已连续工作 D^max 天，则下一天的 act 值域删除 {W}。约束传播是单调的——只删除值、不添加值，保证终止。

### 2.2 分支（Branching）

当约束传播停止且变量尚未完全确定时，CP 求解器选择一个变量进行分支。常用策略是"先选值域最小的变量"（first-fail principle）：优先处理约束最紧的变量，尽早发现冲突、触发回溯。在本题中，分支在"路径骨架"层面进行——递归地尝试 {W1, W2, W3, S1, S2} 中的每个可行选项作为下一个访问点。

### 2.3 回溯（Backtracking）

当某一分支导致约束冲突（某个变量的值域为空，不可能满足所有约束）时，CP 求解器回溯到上一个选择点，尝试其他分支。回溯确保搜索树中的所有可行解都被系统性地探索。

## 三、本题的三种剪枝规则

**规则 1 — 上界剪枝（Bound Pruning）**：在搜索树的任意节点（当前位置 pos，已用天数 t），剩余的最大可能 Z 增益为 UB_Z = (30 - t - dist(pos, E)) * 28，其中 28 是 W3 的日收益。若 Z_current + UB_Z <= best_Z，则以该节点为根的整个子树都不可能产生更优解，直接剪掉。这是 CP 中最强有力的剪枝。

**规则 2 — 相邻去重**：若 path[i] == path[i+1]（如 W3 -> W3），跳过。相邻相同点的距离为 0，不消耗旅行天数却能重置"连续作业"计数，本质上是绕过 max_consec 约束的漏洞。

**规则 3 — 前向检查（Forward Checking）**：在尝试添加点 p 到路径前，检查 travel_sofar + dist(current, p) + dist(p, E) <= 30。若不满足，则从 p 无法在时限内抵达终点，跳过。

## 四、搜索树结构

搜索树以 B 为根节点，每个内部节点分支出 5 个可能的下一访问点。叶节点（路径已确定）枚举工作天数组合并调用贪婪模拟评估。总探索节点数：313（含内部节点和叶节点），最大深度 7。

搜索树输出示例：

| Node | Depth | Action | UpperBound | BestZ |
|------|-------|--------|------------|-------|
| 1 | 0 | branch | 688 | -Inf |
| 1 | 0 | leaf | 0 | 100 |
| 2 | 1 | branch | 576 | 100 |
| 3 | 2 | branch | 464 | 120 |
| ... | ... | ... | ... | ... |

上界随深度递减（688 -> 576 -> 464 -> ...），体现了搜索从"乐观估计"逐渐收紧的过程。

## 五、实验结果

| 指标 | 值 |
|------|:--:|
| 最优 Z | 288 |
| 最优 M | 44 |
| 路径 | B -> W1 -> S2 -> W3 -> S2 -> W3 -> E |
| 探索节点 | 313 |
| 耗时 | 0.03 秒 |

## 六、CP vs 枚举 vs 列生成

| 维度 | 枚举 | 列生成 | 约束规划 |
|------|:---:|:---:|:---:|
| 搜索策略 | 穷举所有骨架 | 迭代添加改进列 | 递归回溯+剪枝 |
| 剪枝机制 | 无（全枚举） | 对偶信息 | 上界+前向检查+去重 |
| 节点数/列数 | 542 骨架 | 16 列 | 313 节点 |
| 最优性保证 | 穷举保证 | 对偶理论保证 | 搜索树保证 |
| 可解释性 | 高 | 中（对偶概念） | 高（搜索树可视化） |
| 代码复杂度 | 低 | 中 | 中 |

## 七、方法评价

**优点**：搜索树透明，每个节点可追踪；三种剪枝大幅减少搜索空间；约束表达直接、接近题目原文；上界剪枝在任务 3（30x30）中更有价值。

**局限**：需手工实现传播和搜索逻辑；递归深度受 MATLAB 限制（默认 500）；无原生 CP 求解器时开发工作量大；对连续采购量的传播力弱于整数变量。

## 八、扩展到任务 3 的考虑

在任务 3（30x30 网格，90 天，天气概率 0.8/0.2）中，搜索空间急剧膨胀。CP 的上界剪枝将成为关键——利用天气概率加权计算期望上界，可以在搜索早期剪掉大量低期望值路径。CP 的框架天然支持加入随机信息：在叶节点评估时调用期望值计算（而非确定性贪婪模拟）。
"""

# Write using Unicode paths
for fname, content in [
    (os.path.join(desktop, '\u5217\u751f\u6210', '\u5217\u751f\u6210\u65b9\u6cd5\u8bf4\u660e.md'), cg_doc),
    (os.path.join(desktop, '\u7ea6\u675f\u89c4\u5212', '\u7ea6\u675f\u89c4\u5212\u65b9\u6cd5\u8bf4\u660e.md'), cp_doc)
]:
    os.makedirs(os.path.dirname(fname), exist_ok=True)
    with open(fname, 'w', encoding='utf-8', newline='\r\n') as f:
        f.write(content)
    print(f'Written {len(content)} chars to {fname}')

print('Done.')